import { SendMailOptions, Transporter } from 'nodemailer';
import { Options as TransportOptions, SentMessageInfo } from 'nodemailer/lib/smtp-transport';
import { ReactElement } from 'react';
export declare type EmailConfig = {
    transport: TransportOptions | Transporter;
    defaults?: SendMailOptions;
};
export declare type Email<Props> = (props: Props) => {
    body: ReactElement<Props>;
    subject: string;
};
export declare type EmailsList = {
    [name: string]: Email<any>;
};
export declare const Mailer: <Emails extends EmailsList>(config: EmailConfig, emails: Emails) => {
    send: <TemplateName extends keyof Emails>(template: TemplateName, props: Parameters<Emails[TemplateName]>[0], options: SendMailOptions) => Promise<SentMessageInfo>;
};
export default Mailer;
